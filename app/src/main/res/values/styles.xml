<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- dialog 按钮样式 -->
    <style name="btn_dialog">
        <item name="android:textColor">@color/navy</item>
        <item name="android:textSize">18sp</item>
        <item name="android:gravity">center</item>
    </style>
    <!--dialog弹出动画-->
    <style name="style_animation" parent="android:Animation">
        <item name="@android:windowEnterAnimation">@anim/anim_in</item>
        <item name="@android:windowExitAnimation">@anim/anim_out</item>
    </style>
    <!--图片选择器-->
    <style name="PhotoActivityTheme">
        <item name="colorTheme">@color/common_botton_bar_blue</item>
        <item name="colorThemeDark">@color/common_botton_bar_blue</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
